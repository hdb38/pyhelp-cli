# pyhelp-cli是什么？
pyhelp-cli是一个命令行工具，提供了一些命令。

## 前置资源
只需要python即可。

## 怎么使用python-cli？
1.把项目克隆到本地
```bash
git clone ...
```
2.进入目录
```bash
cd pyhelp-cli
```
3.安装工具
```bash
pip install -e .
```
4.开始使用
```bash
pyhelp --help
```
